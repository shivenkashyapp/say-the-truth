import React from 'react';

const BackgroundCheck: React.FC = () => {
    return (
        <div>
            <h2>Background check (user/article)</h2>
            <li>
                user has been flagged multiple times on X for spreading
                convoluted info.
            </li>
        </div>
    );
};

export default BackgroundCheck;
